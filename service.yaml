#Service Type nodePort
apiVersion: v1
kind: Service
metadata:
  name: class-service
  labels:
    app: class-app
spec:
  selector:
    app: class-app

  type: NodePort
  ports:
  - nodePort: 31000
    port: 8080
    targetPort: 8080


